# Fns whose parameter count legitimately differs across bindings.
# Read by `cargo xtask coverage`. Format: <fn_name>  <reason>.
# A new arity disagreement NOT listed here fails the gate — so binding drift
# (a param silently added/dropped in one language) is caught. Prefer fixing the
# binding over adding a line here; tracked gaps live in TODO.md (CLI/adaptability).

utc_to_jd        JS packs the 7 date scalars into a UtcDate object (language idiom) — TODO-tracked as DECIDED.
houses_ex        PHP param order is (jd, flags, lat, lon, hsys); Py/JS are (jd, lat, lon, hsys, flags) — BIND-5, breaking to fix.
rise_trans       Python omits ephe_flags and puts flags last; JS/PHP put flags 3rd — BIND-6, breaking to fix.
